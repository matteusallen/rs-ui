// @flow
import React from 'react';
import { withRouter } from 'react-router';

const SetPassword = () => (
  <section>
    <div>SetPassword Screen</div>
  </section>
);

export default withRouter(SetPassword);
