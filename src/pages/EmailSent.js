// @flow
import React from 'react';
import { withRouter } from 'react-router';

const EmailSent = () => (
  <section>
    <div>EmailSent Screen</div>
  </section>
);

export default withRouter(EmailSent);
